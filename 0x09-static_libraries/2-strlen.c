#include <string.h>
#include "main.h"

/**
 * _strlen - function that returns the string length of a string
 * @s: pointer to string input
 * Return: 0 for success
 */

int _strlen(char *s)
{
	return (strlen(s));
}
